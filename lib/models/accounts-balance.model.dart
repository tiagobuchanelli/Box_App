import 'package:flutter/material.dart';

class AccountsBalance {
  String name;
  String price;
  String date;
  String color;
  String type;

  AccountsBalance(this.name, this.price, this.date, this.color, this.type);
}
